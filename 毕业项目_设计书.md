# 毕业项目设计书：垫片尺寸测量 + 缺陷检测 + PLC 分拣

> 目标：把第 1~6 课的知识串成一个可验收的完整小系统

## 1. 需求描述

产线对垫片全检：
- 直径要求：9.00 ± 0.10 mm（超出即 NG）
- 表面不允许有划痕 / 脏污（有即 NG）
- 视觉系统输出 OK / NG，OK 放行、NG 剔除，并统计良率

## 2. 系统设计（双工位，对应真实产线）

| 工位 | 打光 | 干什么 |
|---|---|---|
| 工位 1 | 背光 | 测量直径（毫米） |
| 工位 2 | 前光 | 检测表面缺陷 |

视觉程序流程：模板匹配定位 → ROI 裁剪 → 背光测直径 → 前光查缺陷 → 综合判断 → TCP 发给 PLC

## 3. 标定与判据

- 标定：标准垫片 180 像素 = 9.00mm → 精度 0.05 mm/像素
- 直径公差：±0.10mm（约 ±2 像素）
- 缺陷判据：异常亮斑面积 > 80 像素 → NG
- 报文格式：`判断,直径mm,原因`，例：`OK,9.02,CLEAN` / `NG,9.12,SIZE` / `NG,9.01,DEFECT`

## 4. 模拟产线

- 100 个垫片，位置随机（模拟传送带定位误差）
- 直径在 8.85~9.15mm 随机（部分超差）
- 25% 概率带表面缺陷（位置 / 大小随机）
- 每个样品都有"真值"，用于验收

## 5. 验收标准

- [ ] 正确率 >= 95%（100 个样品）
- [ ] 节拍 < 1000ms / 件
- [ ] 输出：正确率、误杀率（好品判 NG）、漏检率（坏品判 OK）
- [ ] 模拟 PLC 收到结果并执行放行 / 剔除动作

## 6. 运行方法

窗口 1（先启动）：
```
python 练习6_模拟PLC端.py
```
窗口 2：
```
python 毕业项目_主程序.py
```