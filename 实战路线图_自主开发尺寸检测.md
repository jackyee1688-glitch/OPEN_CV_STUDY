# 实战路线图：自主开发"设备上方"尺寸检测视觉系统

> 背景：已完成 7 课 + 毕业项目，掌握 Python/OpenCV 视觉基础与 PLC 通信
> 目标：在设备上方装一套尺寸检测系统，完全自主开发

## 0. 先确认三个参数（决定选型）

1. 工件大小（决定视野 FOV）：如 50mm / 100mm / 200mm
2. 检测精度要求：如 ±0.05mm / ±0.1mm
3. 预算：开发阶段 / 现场整体方案

**选型公式：像素精度 = 视野 ÷ 相机长边像素数**
经验规则：要求像素精度 ≤ 检测精度 ÷ 5（留安全系数）
例：视野 100mm、500万像素(2448) → 0.04mm/px；做 ±0.1mm 检测有余量

## 1. 硬件选型（入门推荐方案）

| 部件 | 推荐 | 预算(元) | 说明 |
|---|---|---|---|
| 工业相机 | 海康 MV-CA050-10GC，500万 GigE 全局快门 | 2000-3000 | 尺寸检测主流，SDK 全中文资料 |
| 镜头 | 12/16mm 定焦 C 口，靶面 2/3" | 300-800 | 先普通定焦+标定补偿畸变；精度极高才上远心 |
| 光源 | 环形光（正面）或 背光（轮廓测量首选） | 200-600 | 尺寸检测：能背光就背光 |
| 光源控制器 | 恒流 / 频闪 | 300-600 | 保证亮度稳定可重复 |
| 标定板 | 圆点标定板或棋盘格 | 50-300 | 畸变校正 + 像素当量，必买 |
| 触发 | 光电 / 接近开关 + 相机 IO 线 | 100 | 硬触发，和 PLC 联动 |
| 工控机 | 开发用现有电脑；现场配无风扇工控机 | 0 / 3000-5000 | 开发阶段不必买 |

**开发阶段总预算约 3000-5000 元（不含工控机）**

## 2. "设备上方安装"的三个现场要点

1. **支架刚性**：相机与工件之间不能有相对震动（螺纹锁紧、加筋、减震）
2. **工作距离固定**：相机高度定死，标定后才不会漂；要求苛刻用远心镜头
3. **环境光隔离**：遮光罩 + 光源频闪，防止车间自然光 / 日光灯干扰

## 3. 还需要学什么（按优先级）

1. **相机 SDK 取流**（海康 MVS SDK，Python/C# 调用）—— 真实拍照的入口
2. **相机标定 + 畸变校正**（cv2.calibrateCamera + 标定板）—— 尺寸精度的地基
3. **亚像素边缘测量**（卡尺工具 caliper / 亚像素边缘）—— 精度从像素级升到亚像素
4. **像素当量与重复性验证**（同工件拍多次看波动，GR&R 思想）—— 证明系统稳定
5. **硬触发 + PLC 集成**（已会 TCP/IP，补硬接线、超时与防错）
6. **工业软件** VisionMaster / Halcon —— 图形化测量，开发效率高
7. **深度学习缺陷检测** —— 以后做外观检测再学

## 4. 六周学习安排（每周 3-5 小时）

| 周 | 主题 | 产出 |
|---|---|---|
| 1 | 采购/借硬件 + SDK 取流 | 电脑上实时显示相机画面 |
| 2 | 标定板标定 + 畸变校正 | 拍到相机内参，图像拉直 |
| 3 | 亚像素测量 | 测直径 / 距离，精度到亚像素 |
| 4 | 重复性验证 + 公差判断 | 同工件拍 20 次，看测量波动 |
| 5 | 硬触发 + PLC 通信 | 传感器触发 → 拍照 → 结果给 PLC |
| 6 | 装机调试 | 设备上方装好，现场跑通并复盘 |

## 5. 两条路线建议

- **路线 A（快速出活）**：海康相机 + VisionMaster 图形化 —— 2-4 周上手测量项目，适合先完成任务
- **路线 B（吃透原理）**：Python + OpenCV + MVS SDK —— 完全自主可控，标定和亚像素要下功夫
- **建议**：B 为主（你已会 OpenCV），A 为辅助（工具提效），两条腿走路

## 6. 下一步可以一起做的

- [ ] 细化采购清单（按你的工件尺寸 / 精度 / 预算给具体型号）
- [ ] 写"棋盘格标定 + 畸变校正"程序模板
- [ ] 把毕业项目升级成"亚像素测量 + 标定 + 重复性验证"框架
- [ ] 整理海康 MVS SDK 官方教程链接