# 海康相机接入指南（真机取流）

## 需要准备的东西

1. 海康工业相机一台（GigE 千兆网口 或 USB3）
2. 电脑安装 **MVS**（海康机器视觉软件，含驱动和 SDK）
   - 官网：vision.hikrobotics.com → 支持中心 → 下载 MVS
   - 或相机附带的安装光盘
3. Python 封装：`pip install mvsdk`

## 接入步骤

1. 安装 MVS 后打开它，确认能"看到"相机
   - GigE 相机第一次使用必须先设置 IP（见下）
   - 能看到画面 = 驱动正常
2. 命令行执行：`pip install mvsdk`
3. 打开 视觉软件_最小版.py → 相机类型选「海康工业相机」→ 点「打开相机」
4. 正常就出画面，然后可以「相机拍照测量」

## GigE 相机 IP 设置（最容易卡住的一步）

- 相机默认 IP 通常是 192.168.1.x，你的电脑网卡是别的网段就搜不到
- 办法一：把电脑网卡设成同网段静态 IP（如 192.168.1.10，子网 255.255.255.0）
- 办法二：MVS 里用「静态IP / 动态IP」工具直接改相机 IP
- 详细见 MVS 用户手册「相机网络配置」

## 常见报错对照

| 报错 | 原因 | 解决 |
|---|---|---|
| 未找到 mvsdk | 没装 pip 包 | pip install mvsdk |
| 没有发现海康相机 | 网线/驱动/IP 问题 | 先让 MVS 能看到相机 |
| 取流中断 / 超时 | 带宽不足或网线松动 | 用千兆网线，检查连接 |
| 画面全黑/花屏 | 增益/曝光没调，或触发没设置 | 在代码或 MVS 里调曝光增益 |

## 代码里怎么改（对应架构）

- 相机代码集中在 `视觉软件_最小版.py` 的「采集层」段
- 换触发方式：`MV_CC_SetEnumValue("TriggerMode", 1)` = 软触发，
  再用 `MV_CC_SetCommandValue("TriggerSoftware")` 发一次触发
- 相机分辨率/尺寸和模拟图不同：拿到真机后，测量前要先做「标定」
  （下一项升级：棋盘格畸变校正 + 像素当量）