# Python 开发视觉软件评估与路线图

## 一、先分清两个层次

1. **单机版检测程序**（自己的设备用 / 公司内部用）—— 现实可行，值得做
2. **通用视觉平台产品**（像 VisionMaster / Halcon 那样的通用软件）—— 不要单干，那是公司级产品

你要做的是第 1 种。

## 二、一个"Python 视觉软件"由什么组成

```
┌─ UI 层    : PySide6/PyQt5（参数设置、图像显示、结果表格、方案管理）
├─ 业务层   : 流程控制（触发 → 采集 → 定位 → 测量 → 判定 → 输出）
├─ 算法层   : OpenCV + numpy（模板匹配 / 找线 / 亚像素测量 / Blob）
├─ 采集层   : 海康 MVS SDK / pypylon / OpenCV VideoCapture
├─ 通信层   : TCP/IP、Modbus(pymodbus)、串口(pyserial)
├─ 数据层   : SQLite / CSV 结果记录、日志
└─ 部署     : PyInstaller 打包成 exe，免安装
```

## 三、技术栈与难度

| 部分 | 用到的库 | 难度 | 说明 |
|---|---|---|---|
| 界面 | PySide6 / PyQt5 | 中（1-2周） | 最大的新东西 |
| 图像算法 | OpenCV, numpy | 你已会 | 毕业项目就是雏形 |
| 相机取流 | 海康 MVS Python SDK | 中 | 官方有 Python 示例 |
| 标定+畸变 | cv2.calibrateCamera | 需补 | 尺寸精度地基 |
| 亚像素测量 | 自己写卡尺/边缘拟合 | 中高 | 或用 Halcon Python 版 |
| 通信 | socket, pymodbus, pyserial | 低 | TCP 你已会 |
| 数据/日志 | sqlite3, csv | 低 | |
| 打包部署 | PyInstaller | 低 | |

## 四、难度评估（针对你的基础）

- **算法部分你已经会了**：毕业项目 = 采集 → 定位 → 测量 → 缺陷 → 通信的最小闭环
- 真正的难点不是写代码，是**工程化和鲁棒性**：
  - 相机打不开 / 丢帧 / 超时怎么办（异常处理）
  - 现场光线变了测量还准不准（稳定性）
  - 换料号时参数怎么管理（方案/配置）
- 需要补的三块：**相机 SDK 取流、标定+畸变校正、UI 界面**

## 五、时间估计（每天 2-3 小时）

- 可运行的最小测量软件（界面 + 取流 + 测量 + 结果）：**3-4 周**
- 加方案管理 / 多相机 / 报表 / 打包：**1-2 个月**
- 现场稳定运行 + 持续改进：**3-6 个月**
- 对比：VisionMaster 同样功能 1-2 周

## 六、Python vs VisionMaster

| 对比项 | Python 自研 | VisionMaster |
|---|---|---|
| 出活速度 | 慢（3-4周起） | 快（1-2周） |
| 定制化 | 完全自由 | 受模块限制 |
| 授权费 | 无 | 随相机 / 加密狗 |
| 算法成熟度 | 自己保证 | 厂家保证 |
| 深度学习 | 无缝接入 | 支持有限 |
| 原理理解 | 透彻（全在手里） | 黑盒 |

## 七、推荐策略（两条腿走路）

1. **主业用 VisionMaster**：快速交付，供应商方案直接能维护
2. **Python 做 VisionMaster 做不了的**：疑难图像分析、深度学习、特殊算法原型、数据报表
3. **进阶机会**：非标设备厂家很需要"能写定制视觉软件"的人——这会成为你的差异化竞争力

## 八、如果要做，从哪开始（建议顺序）

1. 相机 SDK 取流（海康 MVS Python 示例）
2. 标定程序模板（棋盘格 + 畸变校正）
3. 最小 GUI：打开相机 → 显示画面 → 按钮触发 → 显示测量结果
4. 加方案保存 / 结果记录 / 通信
5. PyInstaller 打包